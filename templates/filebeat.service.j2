[Unit]
Description=Filebeat in Docker
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/docker start -a {{ filebeat_service }}
ExecStop=/usr/bin/docker stop -t 20 {{ filebeat_service }}

[Install]
WantedBy=multi-user.target
